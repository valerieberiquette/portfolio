const terminalPopup = document.getElementById('terminalPopup');
const terminalForm = document.getElementById('terminalForm');
const terminalInput = document.getElementById('terminalInput');
const terminalStorageKey = 'portfolioTerminalUnlocked';

/*
if (localStorage.getItem(terminalStorageKey) === 'true') {
    terminalPopup.classList.add('is-hidden');
} else {
    terminalInput.focus();
}

terminalForm.addEventListener('submit', function (event) {
    event.preventDefault();

    if (terminalInput.value.trim().toLowerCase() === 'openportfolio') {
        localStorage.setItem(terminalStorageKey, 'true');
        terminalPopup.classList.add('is-hidden');
    }
});
*/

